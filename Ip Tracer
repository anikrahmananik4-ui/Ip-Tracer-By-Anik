<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>IP Tracer by Anik</title>

<link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@400;700&display=swap" rel="stylesheet">
<link rel="stylesheet" href="https://unpkg.com/leaflet/dist/leaflet.css">

<style>
body{
 margin:0;
 background:black;
 font-family:'Orbitron',monospace;
 color:#00ffe7;
}
header{
 text-align:center;
 padding:15px;
 border-bottom:1px solid #00ffe7;
 box-shadow:0 0 25px #00ffe7;
}
.container{
 max-width:420px;
 margin:auto;
 padding:15px;
}
.card{
 border:1px solid #00ffe7;
 box-shadow:0 0 15px #00ffe7;
 padding:12px;
 margin-bottom:15px;
}
h3{margin:5px 0;font-size:15px}
p{font-size:13px;margin:4px 0}
button,input{
 width:100%;
 margin-top:6px;
 background:black;
 color:#00ffe7;
 border:1px solid #00ffe7;
 padding:8px;
 font-family:'Orbitron';
}
button:hover{background:#00ffe7;color:black}
#map{height:220px;margin-top:10px}
#pulse{
 height:10px;
 width:0%;
 background:#00ffe7;
 box-shadow:0 0 20px #00ffe7;
}
</style>
</head>

<body>

<header>
<h2>🌐 IP Tracer by Anik</h2>
<small>Cyber Intelligence Panel</small>
</header>

<div class="container">

<!-- IP TRACER -->
<div class="card" id="ipinfo">
<h3>📍 IP Tracer</h3>
<p>Detecting...</p>
</div>

<!-- CUSTOM IP -->
<div class="card">
<h3>🔎 Custom IP Lookup</h3>
<input id="cip" placeholder="Enter IP address">
<button onclick="customIP()">Search</button>
<div id="cipout"></div>
</div>

<!-- CONNECTION INTELLIGENCE -->
<div class="card" id="conn">
<h3>📡 Connection Intelligence</h3>
</div>

<!-- PRIVACY RADAR -->
<div class="card" id="privacy">
<h3>🧠 Privacy Radar</h3>
</div>

<!-- CYBER SIGNAL -->
<div class="card">
<h3>🛰 Cyber Signal Pulse</h3>
<div id="pulse"></div>
</div>

<!-- MAP -->
<div class="card">
<h3>🗺 Location Map</h3>
<div id="map"></div>
</div>

</div>

<script src="https://unpkg.com/leaflet/dist/leaflet.js"></script>

<script>
// ================= IP TRACER =================
async function myIP(){
 let ip = await fetch("https://api.ipify.org?format=json").then(r=>r.json());
 let d = await fetch("https://ipapi.co/"+ip.ip+"/json/").then(r=>r.json());

 document.getElementById("ipinfo").innerHTML = `
 <h3>📍 IP Tracer</h3>
 <p>IP: ${d.ip}</p>
 <p>Country: ${d.country_name}</p>
 <p>City: ${d.city}</p>
 <p>ISP: ${d.org}</p>
 <p>Timezone: ${d.timezone}</p>
 `;
}
myIP();

// ================= CUSTOM IP =================
async function customIP(){
 let ip = document.getElementById("cip").value;
 let d = await fetch("https://ipapi.co/"+ip+"/json/").then(r=>r.json());
 document.getElementById("cipout").innerHTML = `
 <p>Country: ${d.country_name}</p>
 <p>City: ${d.city}</p>
 <p>ISP: ${d.org}</p>
 `;
}

// ================= CONNECTION INTELLIGENCE =================
const c = navigator.connection || {};
document.getElementById("conn").innerHTML += `
<p>Type: ${c.effectiveType || "N/A"}</p>
<p>Downlink: ${c.downlink || "N/A"} Mbps</p>
<p>RTT: ${c.rtt || "N/A"} ms</p>
<p>Save Data: ${c.saveData || false}</p>
<p>Status: ${navigator.onLine ? "Online" : "Offline"}</p>
`;

// ================= PRIVACY RADAR =================
document.getElementById("privacy").innerHTML += `
<p>Cookies Enabled: ${navigator.cookieEnabled}</p>
<p>Do Not Track: ${navigator.doNotTrack}</p>
<p>LocalStorage: ${!!window.localStorage}</p>
<p>SessionStorage: ${!!window.sessionStorage}</p>
<p>IndexedDB: ${!!window.indexedDB}</p>
`;

// ================= CYBER SIGNAL =================
let w = 0;
setInterval(()=>{
 w = (w + Math.random()*25) % 100;
 document.getElementById("pulse").style.width = w + "%";
},600);

// ================= MAP =================
let map = L.map('map').setView([0,0],2);
L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png').addTo(map);

navigator.geolocation.getCurrentPosition(pos=>{
 let lat = pos.coords.latitude;
 let lon = pos.coords.longitude;
 map.setView([lat,lon],12);
 L.marker([lat,lon]).addTo(map).bindPopup("Your Location").openPopup();
});
</script>

</body>
</html>